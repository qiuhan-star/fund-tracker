# 📊 基金追踪器 重构与优化清单（终极整合版）

> **记录日期**：2026-08-05
> **状态**：✅ 原 18 项已全部收官。已落地 11 项 · 经核查已满足 6 项 · 保持现状 1 项（债基动态化，硬改会清空债券专区）。最终 commit：P1=`1b377a9`、P2=`0bc6c06`、P3=`6b62213`、计划文档=`534e842`（本地 main 领先 origin/main，待 push + 硬刷新）。
> **配套记忆**：`../.workbuddy/memory/MEMORY.md`「重构蓝图」段。下方「新需求/待办」区用于追加后续要改的 list。

---

## 📋 状态总览（一眼看清哪些是办了的、哪些待办）

**图例**：✅ 已实装 ｜ ✅(已满足) 经核查已满足、无需改 ｜ ⏸ 保持现状（有意不动） ｜ ⬜ 待办

| 项 | 标题 | 优先级 | 状态 |
| --- | --- | --- | --- |
| 1 | 三维解耦（cat/tags/account） | P0 | ✅ |
| 2 | 债券模块动态化 | P2 | ⏸ 保持现状 |
| 3 | 单一事实原则（trades 驱动） | P2 | ✅(已满足) |
| 4 | 手续费字段 + 分红类型（轻量预留） | P2 | ✅(已满足) |
| 5 | 每日快照时间序列 daily_snapshots | P2 | ✅ |
| 6 | Schema 版本控制 + 迁移 | P0 | ✅ |
| 7 | 多币种/汇率轻量预留 | P3 | ✅ |
| 8 | 补全截断代码 + 闭合标签 | P0 | ✅(已满足) |
| 9 | 眼睛图标绑定总资产 | P1 | ✅ |
| 10 | 大盘看板隔离 + 分区精简 | P2 | ✅ |
| 11 | 2 行高密度扁平列表 | P0 | ✅(已满足) |
| 12 | 数字等宽 Tabular Nums 对齐 | P1 | ✅(已满足) |
| 13 | 卖出成本 FIFO 重算 | P1 | ✅ |
| 14 | 净值状态 [估]/[确] 标记 | P1 | ✅ |
| 15 | 渠道/账户多维联动 | P1 | ✅(已满足) |
| 16 | 上下文快捷记账抽屉 | P3 | ✅ |
| 17 | 分析 Tab Donut 紧凑化 + 下钻 | P3 | ✅ |
| 18 | 持仓列表动态排序筛选 | P3 | ✅ |

---

## 🆕 新需求 / 待办（待补充）

> 后续要改的 list 直接贴这里，按上表格式追加即可（状态先标 ⬜，做完改 ✅）。

| # | 需求 | 优先级 | 状态 | 备注 |
| --- | --- | --- | --- | --- |
| — | （待补充 — 用户将提供新的修改清单） | — | — | — |
| 19 | 刷新后视图联动补全（状态联动断层） | P3 | ✅ | `refresh()` 末尾补 `renderHomeList()` + `activeTab===1` 时 `renderPie()`；解决刷新后持仓列表/饼图不更新。`flashMap` 现已作用于列表行（▲▼闪）。无需重写引擎（持仓仅个位数，批量 JSONP 已聚合）。 |
| 20 | 移除「个股追踪」Tab 及相关代码 | P3 | ✅ | 用户不需要。删 mt4 按钮+tp4 面板+switchTab/render 的 `activeTab===4` 分支+整个「个股追踪」代码块（buildStocksView/fetchStockQuotes/renderStocks/loadStockSample 等，共 153 行）。与基金共用 trades/snaps、以 type:'stock' 区分，移除只去掉 UI，账本数据不动；用户确认无个股数据故无需 purge。`parseQt` 因市场风向 Tab 复用而保留。commit `d7307f0`。 |
| 21 | 盈亏分布柱状图标签溢出/缺失 | P2 | ✅ | 根因是 `.chart-box` 固定 `height:230px`，21 只基金(13px 柱+字号)需 ~378px → Y 轴标签被裁切。修复：`renderBar()` 动态设容器高度 `Math.max(200, sorted.length*18+12)px`（保持 Chart.js 交互，未按用户建议改写 HTML/CSS flex）。 |
| 22 | 概览「基金数 / 盈:亏」对不上总数 | P2 | ✅ | `win=f.ret>0` / `lose=f.ret<0` 计算时漏掉 `ret===0`（债基/未结算）的"平"档。修复：增 `flat=f.ret===0` 计数，卡片改为 `N 只 · X盈/Y亏/Z平`（如 21 只·5盈/12亏/4平，合计回 21）。 |
| 23 | 资产配置饼图配色语义冲突（芯片红） | P2 | ✅ | 饼图调色板首色 `#e24b4a`(红) + `CAT_COLORS['芯片']='#e24b4b'`(红)，与"红涨绿跌"撞义。修复：`CAT_COLORS` 与饼图改用**中性分类调色板**（蓝/橙/青/紫/赭/金/石板灰，无红绿）；饼图由 `labels.map(l=>CAT_COLORS[l])` 取色，与对比饼图(当前/建议)一致。 |
| 24 | ECharts 离线白屏（板块面积图无降级） | P2 | ✅ | 用户离线/国内网络拉不到 ECharts CDN → `renderSectorTree` 原只显示错误文案。修复：先算 `rows` 再分支；`typeof echarts==='undefined'` 时渲染 **CSS 纯静态降级**（横向条按市值占比，颜色按加权收益红涨绿跌），新增 `.tree-fallback/.tf-row/.tf-bar/.tf-cap` 样式。联网时仍走 ECharts treemap。 |

---

## 📝 用户反馈整合待办清单（v5 · 2026-08-05）

> 来源：用户多轮贴的 UI/功能/清仓模块反馈。经逐条代码核验，剔除「已存在项」与「否决项」，重排为 7 类 26 条。**协作约定**：仅在本对话逐条改 `index.html`，动手前先 `cp 仓库→沙箱` 对齐 HEAD；红绿=红涨绿跌（A股惯例）。

**已剔除（不再做）**
- *已存在*：账户切换(scope-bar)、实时净值接口(JSONP)、CSV模板下载、累计收益真实曲线、止盈止损阈值自定义、优化建议具体内容、清仓年化列、基金分红FIFO、净值缓存(navCache)、离线支持、清仓流水联动、清仓资产实时更新、清仓统计(总收益/次数)
- *已否决*：均值-方差模型（过度工程）、邮件推送（纯静态文件无服务端）、清仓时机建议（市场择时不可严谨）

| # | 类别 | 需求 | 成本 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | UI操作组织 | 操作项按功能分组（日常/数据/危险分隔线） | 低 | ⬜ |
| 2 | UI操作组织 | CSV 说明改悬浮提示（已在导入下方，补 hover） | 低/可选 | ⬜ |
| 3 | 核心计算与数据 | 收益率标注「持有收益率」+ 公式 | 极小 | ⬜ |
| 4 | 核心计算与数据 | 收益时间维度（先做持有以来/今年以来；近1月/3月/1年暂缓） | 中/部分 | ⬜ |
| 5 | 核心计算与数据 | 自动刷新定时器（每1–3分钟，控频） | 低 | ⬜ |
| 6 | 核心计算与数据 | 每日收盘自动快照（定时器+跳非交易日） | 中 | ⬜ |
| 7 | 功能模块深化 | 资产配置二级分类 + 收益贡献度 | 中 | ⬜ |
| 8 | 功能模块深化 | 目标配置对比 + 偏离预警 | 中 | ⬜ |
| 9 | 功能模块深化 | 板块 Treemap 钻取（点板块看持仓） | 低 | ⬜ |
| 10 | 功能模块深化 | 板块热力时间对比（昨日/上周同期） | 中 | ⬜ |
| 11 | 功能模块深化 | 优化建议增强（每条附预期影响） | 低 | ⬜ |
| 12 | 交互与体验 | 持仓列表行内悬浮气泡（收益率/市值占比） | 低 | ⬜ |
| 13 | 交互与体验 | 卡片视图展开显示最近3笔交易（迷你K线暂缓） | 低/部分 | ⬜ |
| 14 | 交互与体验 | CSV 拖拽上传 | 低 | ⬜ |
| 15 | 交互与体验 | 导出持仓 CSV 含分析列 | 低 | ⬜ |
| 16 | 交互与体验 | 止盈止损站内通知 | 低 | ⬜ |
| 17 | 市场行情增强 | 资金流向排行（TOP5板块主力净流入/流出，东方财富API） | 中 | ⬜ |
| 18 | 数据安全与终端UX | 快照容灾（导入/清空前自动备份 v1~v3 + 损坏自恢复） | 低 | ⬜ |
| 19 | 数据安全与终端UX | 交易时段状态灯（接出 🟢/🟡/⚪） | 极低 | ⬜ |
| 20 | 数据安全与终端UX | 自定义列显隐（紧凑表 ⚙️ 列设置） | 低-中 | ⬜ |
| 21 | 清仓模块增强 | 清仓预览（卖出确认：预计收益/税费） | 低-中 | ⬜ |
| 22 | 清仓模块增强 | 清仓明细补「卖出价格/税费」列 | 低 | ⬜ |
| 23 | 清仓模块增强 | 清仓统计补「平均收益率」卡 | 低 | ⬜ |
| 24 | 清仓模块增强 | 清仓影响分析（对整体配置影响） | 中 | ⬜ |
| 25 | 清仓模块增强 | 收益对比（清仓 vs 继续持有近似） | 中/部分 | ⬜ |
| 26 | 清仓模块增强 | 自动税费计算（赎回费按持有期） | 中 | ⬜ |

**暂缓（缺历史净值序列）**：近1月/3月/1年收益、迷你K线、年化波动率

**建议实施顺序**：本地底座先上 **18→19→20→21**（无外部依赖、服务严谨记账）；17 东方财富API 作独立沙箱实验并行探路不阻塞；其余按成本由低到高穿插。

---

<details>
<summary>📂 原 18 项详细方案（已解决，点击展开）</summary>

## 一、 数据与分类架构问题

### 1. 分类维度混乱（解耦行业、工具与渠道）

* **问题描述**：原 `cat` 字段混杂了行业/主题（如"芯片"、"医药"）、投资工具（如"QDII"）以及资金渠道（如"支付宝"、"微信"）。
* **逻辑隐患**：
  * **板块分析失真**：`广发港股创新药(QDII)` 被归为 `QDII`，导致计算"医药板块"总仓位时遗漏。
  * **渠道标签成为死数据**：标记了"支付宝/微信/小号"，但在持仓列表和分析图表中无联动，无法按平台查看资金分布。
* **优化方案**：采用**三维解耦架构**：
  * **主分类 (`cat`)**：仅代表行业/资产大类（如：`芯片`、`周期资源`、`医药`、`债基`）。
  * **属性标签 (`tags`)**：标示市场与工具类型（如：`tags: ["QDII", "港股"]`）。
  * **账户/渠道 (`account`)**：独立存储资金所在平台（如：`account: "支付宝"`、`"微信"`、`"小号"`）。

### 2. 债券模块动态化（解耦硬编码）

* **问题描述**：`BOND_FUNDS` 静态硬编码了 7 只债基，与 `snaps` 持仓快照中的实际持仓脱节。
* **逻辑隐患**：持仓发生变动时，债券专区 Tab 无法联动更新。
* **优化方案**：移除静态数组，改为根据主持仓数据**动态过滤**：
```javascript
function getBondFunds() {
  return _allHoldings.filter(f => f.cat === '债基' || f.cat === '混合');
}
```
* **实际处置**：⏸ **保持现状**——债基走独立 `bondState` 体系、不在 `snaps`/`_allHoldings`；按 `_allHoldings.filter(cat==='债基')` 会清空债券专区。如需联动应单开 `bondHoldings` 录入入口（新增功能，非重构）。

### 3. 数据源单一事实原则（Single Source of Truth）

* **问题描述**：JSON 保持 `snaps`（静态持仓份额/成本）与 `trades`（买卖流水）双轨运行。
* **逻辑隐患**：新增交易记录后，若 `snaps` 中的市值（`mv`）与成本价（`costPx`）未同步重算，两套数据将产生偏差。
* **优化方案**：以 `trades` 为唯一数据源，自动推算持仓份额、平均成本与已实现盈亏，`snaps` 仅作为盘中实时净值缓存。
* **实际处置**：✅(已满足) — `buildFundsView` 有交易流水的基金本就以 `trades` 推算；彻底移除 `snaps` 持仓角色会让大量纯快照基金丢市值，不做破坏性重写。

### 4. 交易类型与费用结构扩展（手续费与分红预留）

* **问题描述**：现有流水仅记录买卖总额，缺少申购/赎回手续费记录，且无法处理基金分红。
* **逻辑隐患**：扣除申购费后的实际入账份额与实扣金额对不上；分红时会导致账面总收益计算偏低或成本被污染。
* **优化方案**：`trades` 增加手续费字段与扩展交易类型（做轻量预留）：
```json
{
  "id": "t_001",
  "fundCode": "000001",
  "type": "BUY",
  "grossAmount": 10000.00,
  "fee": 10.00,
  "date": "2026-03-15"
}
```
* **实际处置**：✅(已满足) — 记账表单已有 `费用(元)` 输入框写入 `t.fee`（FIFO 已用），`TRADE_TYPES` 已含 现金分红/红利再投/拆分/转换转入/转出。

### 5. 时间序列数据模型（记录每日快照，支撑历史收益曲线）

* **问题描述**：数据结构只存"当前一刻"的资产状态，缺失历史数据。
* **逻辑隐患**：后续无法画出"近1个月资产走势图"、"累计收益率曲线"或计算"历史最大回撤"。
* **优化方案**：建立轻量时间序列表 `daily_snapshots`，每日盘后更新一条结存记录：
```json
{
  "date": "2026-08-05",
  "totalAssets": 158420.50,
  "totalProfit": 12400.30,
  "dayProfit": 850.20
}
```

### 6. Schema 版本控制与迁移机制（防御本地数据升级崩溃）

* **问题描述**：重构时修改了 JSON 结构（如字段改名/拆分），但用户的 `localStorage` 仍保留着旧版数据。
* **逻辑隐患**：升级后前端读取旧数据时会因 `undefined` 字段直接报错，导致页面白屏崩溃。
* **优化方案**：在数据根节点增加 `schemaVersion`，并配置 Auto-Migration 适配器函数，自动将旧版本 JSON 平滑升级至最新结构：
```json
{
  "schemaVersion": "2.0.0",
  "updatedAt": "2026-08-05T00:00:00Z",
  "trades": [...],
  "snaps": [...]
}
```

### 7. 多币种与汇率快照（轻量扩展预留）

* **问题描述**：QDII 基金以人民币计价展示，但底层资产受离岸汇率影响。
* **优化方案**：在持仓数据中预留 `currency: "CNY"` 标记，无需额外复杂计算，仅作未来的跨市场拓展接口。

### 8. 代码结构完整性

* **问题描述**：代码在 `renderBond()` 函数内部被截断，缺失闭合标签。
* **优化方案**：补齐 `renderBond()` 完整逻辑，并在结尾添加 `</script></body></html>` 闭合标签。
* **实际处置**：✅(已满足) — 经核对当前 `index.html` 已完整闭合，无需再改。

## 二、 UI / UX 布局与视觉

### 9. 隐私开关（眼睛图标）就近绑定

* **问题描述**：孤立置于最顶部通用 Header 易导致作用域模糊（误以为是系统设置），且违背"就近操作"原则。
* **优化方案**：
  * **视觉绑定**：将眼睛图标直接贴在 **"总资产 (元)" 标题旁边**，形成 `总资产 [👁️]` 语义组合。
  * **全局响应**：点击闭眼后全站所有 Tab 的敏感金额同步替换为 `****`，并写入 `localStorage` 保持状态。

### 10. 大盘看板降维与市场分区精简

* **问题描述**：每个 Tab 重复展示大盘挤占黄金版面；且当前展示的 14 个大盘中混入超 40% 的破损/无数据卡片（如 `—`），排版缺角混乱。
* **优化方案**：
  * **场景降维**：大盘看板**仅保留在"持仓总览/首页"**，交易和债基 Tab 彻底移除。
  * **精简分区**：剔除无用外盘，仅保留 6~8 个核心指数，按市场划分为 **[A 股区]**、**[港股区]**、**[美股区]** 横向紧凑呈现，实现"零点击、一目了然"。

### 11. 废弃独立卡片，改用 2 行高密度扁平列表（Flat Divider List）

* **问题描述**：独立卡片外框（Cards UI）间距过大、占用 3 行高度，信息密度低且视线割裂。
* **优化方案**：
  * **结构扁平化**：移除卡片外框与外边距，收纳于统一容器中，行与行间仅保留 `1px` 微弱分割线。
  * **严格限制 2 行高度**：
    * **第一行（核心资产）**：左侧【基金名称】，右侧加粗【当前市值】。
    * **第二行（明细与收益）**：左侧【`[行业]` `渠道` · `持仓份数` · `成本总额`】（无份额时展示 `— 份` 确保卡片等高），右侧【`±绝对盈亏金额 (±百分比%)`】。
* **实际处置**：✅(已满足) — `renderHomeList` 已是扁平列表，无需再改。

### 12. 数字开启等宽字体（Tabular Nums）与绝对对齐

* **问题描述**：默认字体下数字 `1` 与 `8` 宽度不一致，导致右侧金额在纵向上错落不齐，数据跳动时产生视觉抖动（Layout Shift）。
* **优化方案**：
  * CSS 中对所有金额、持仓量、收益率强制开启等宽数字及右对齐规范：
```css
.financial-num {
  font-variant-numeric: tabular-nums;
  text-align: right;
}
```

## 三、 核心业务逻辑硬伤

### 13. 减仓/卖出时的成本价重算（FIFO 机制）

* **问题描述**：部分减仓时若使用简单摊薄算法（剩余总成本 = 原总成本 - 卖出金额），高盈利卖出可能导致剩余总成本被扣成**负数**，引发收益率公式计算报错。
* **优化方案**：采用**先进先出法（FIFO）**。卖出时优先结转早期买入成本，独立计算"已实现盈亏（Realized P&L）"，剩余持仓的单位成本价保持不被污染。

### 14. 净值状态标识（盘中估值 vs 盘后确权）

* **问题描述**：场外基金官方净值晚间（19:00–22:00）才定格，盘中仅为预估净值。缺乏状态标记易引发误判。
* **优化方案**：在收益率与金额旁增加状态时间戳标签（如：`[估 14:30]` 或 `[确 08-04]`）。

## 四、 多维联动与交互体验（UX）

### 15. 渠道/账户（支付宝/微信/小号）多维联动

* **问题描述**：设置了账户标记但在其他板块无感知，数据沦为死数据。
* **优化方案**：
  * **持仓 Tab**：顶部增加"账户切片"组件（`[全部]` `[支付宝]` `[微信]` `[小号]`），支持快速按平台过滤。
  * **分析 Tab**：新增"渠道/资金平台分布"看板，展示各平台的资金沉淀比例。
  * **交易 Tab**：记账表单中强制选择"交易账户"，方便后续做平台间资金对账。
* **实际处置**：✅(已满足) — 账户切片 + 渠道看板 + 记账选账户均已具备。

### 16. 上下文快捷记账抽屉（Bottom Sheet / Side Drawer）

* **问题描述**：在持仓列表看到大跌欲补仓时，需切换 Tab 重新搜索；或弹出全屏 Modal 遮挡背后的持仓参考数据。
* **优化方案**：持仓列表每行增加快捷入口，点击触发**半屏抽屉**（移动端底部弹出 60% 高度，桌面端弹出右侧边栏）；抽屉顶部置顶回显当前持仓数据，边看持仓边录入。

### 17. "分析 Tab"紧凑化与反向下钻

* **问题描述**：巨大饼图占满整屏导致图文脱节，且图表属于静态展示无法交互。
* **优化方案**：
  * **图表紧凑化**：将传统大饼图重构成 **环形图（Donut Chart）+ 侧边图例（Legend）** 的紧凑组合。
  * **反向下钻**：点击环形图中的某一扇区（如"芯片"），下方列表自动下钻筛选并高亮属于该分类的所有持仓明细。

### 18. 持仓列表动态排序与筛选

* **问题描述**：持仓基金数量增多后，静态列表检索效率低。
* **优化方案**：持仓列表顶部增加极简排序组件（按持仓市值高低、今日涨跌幅榜、累计收益率等排序）。

</details>

<details>
<summary>📂 重构优先级路线图（点击展开）</summary>

| 模块 | 优化项 | 解决的核心问题 | 优先级 |
| --- | --- | --- | --- |
| **数据架构** | 1. 行业/工具/渠道三维解耦 | 解决板块集中度失真、**激活支付宝/微信等渠道数据** | **P0（最高）** |
| **代码规范** | 8. 补全闭合标签与截断函数 | 修复语法报错，保证页面运行稳定 | **P0（最高）** |
| **UI 布局** | 11. 改用 2 行高密度扁平列表 | 废弃笨重独立卡片，提升屏效与阅读连贯感 | **P0（最高）** |
| **数据防御** | 6. Schema 版本控制与迁移 | 防范重构后旧数据导致的**白屏崩溃** | **P0（最高）** |
| **UI 视觉** | 9. 眼睛图标绑定至"总资产"旁 | 优化视觉亲密性与就近操作路径 | **P1（高）** |
| **UI 视觉** | 12. 数字等宽（Tabular Nums）对齐 | 解决金额右侧纵向错位与数据跳动抖动 | **P1（高）** |
| **业务逻辑** | 13. 引入卖出成本 FIFO 重算逻辑 | 规避减仓导致总成本变为负数的计算报错 | **P1（高）** |
| **业务逻辑** | 14. 标记 `[估]` / `[确]` 净值状态 | 消除盘中预估与盘后真实确权的认知模糊 | **P1（高）** |
| **多维联动** | 15. 渠道/账户（支付宝/微信）联动 | 在持仓与分析 Tab 提供按平台筛选与占比看板 | **P1（高）** |
| **布局优化** | 10. 大盘看板场景隔离与市场分区 | 清理破损/无用外盘，保留 A/港/美 核心指数分区 | **P2（中）** |
| **数据架构** | 2. 动态债基 + 3. 交易驱动计算 + 4. 扣费记录 | 消除数据冗余，实现精准对账与单向数据流 | **P2（中）** |
| **数据架构** | 5. 每日快照时间序列建模 | 为后续绘制**历史资产与收益走势曲线**打好底座 | **P2（中）** |
| **交互体验** | 16. 上下文快捷记账抽屉 (Drawer) | 解决记账断层，弹抽屉的同时可看当前持仓上下文 | **P3（低）** |
| **交互体验** | 17. 分析 Tab 紧凑化与下钻 / 18. 动态排序 | 采用 Donut 图+侧边图例，提高分析下钻与检索效率 | **P3（低）** |
| **数据架构** | 7. 多币种/分红类型轻量预留 | 预留接口，保证后续扩展时无需再次动底层结构 | **P3（低）** |

</details>

<details>
<summary>📂 实施记录（每次落地的验证与说明，点击展开）</summary>

> ✅ **已实施**：第 6 项 Schema 版本控制（2026-08-05）。数据根新增 `schemaVersion:'2.0.0'` 与 `updatedAt`；新增 `migrate()` 适配器，旧版无版本号平铺数据加载时自动升级并回写 localStorage，损坏数据安全兜底为空结构，避免白屏崩溃。后续第 2/3/4/5 项重构只需往 `MIGRATIONS` 数组追加步骤即可平滑升级。

> **三项关键底座工程（用户确认必做）**：④ 交易手续费（精准对账）、⑤ 每日快照时间序列（历史曲线）、⑥ Schema 版本控制（防白屏）。
> **两项轻量化结构预留（不增加录入负担）**：④ 分红类型、⑦ 跨境 QDII 汇率换算。

> **P0 核查更新（2026-08-05）**：第 6 项已实施 ✅；第 8 项（renderBond 截断）与第 11 项（2 行扁平列表）经核对当前 `index.html` 已满足清单要求（renderBond 完整闭合、renderHomeList 已是扁平列表），无需再改；P0 实际剩余仅第 1 项（三维解耦）。
> **第 1 项核心原则（用户明确）**：`cat`（行业）必须基于基金**真实十大重仓的底层资产板块**判断，不能凭基金名称猜测。

> ✅ **已实施**：第 1 项 三维解耦（2026-08-05）。`cat` 改为纯行业分类，旧 `QDII` 误分类按**底层持仓**重定（017731/026755/022184→芯片、019671→医药、020989→科技、019449/016453→海外），并新增 `tags`（QDII/全球/港股/美股/日本…）承载工具与市场属性；`account` 维持渠道维度。改动点：① SAMPLE_SNAP 7 只 QDII + `fund-data.json` 真实数据同步重分类并加 `tags`；② CSS 新增 `.tag-海外`/`.tag-attr`，首页列表与详情页渲染 `tags`；③ `compute()` 把 `snap.tags` 带入持仓对象 `f.tags`；④ 风险引擎 `RISK_CATS` 去掉 `QDII` 改 `海外`、`CAT_COLORS` 补 `海外`；⑤ 跨境上限逻辑从 `cat==='QDII'` 改为按 `tags` 判断——新增 `qdiiShare()` + 分析 Tab「跨境(QDII)仓位偏高」独立卡片；⑥ `guessCat` 不再吐 `QDII`（跨境宽基→海外，港股/QDII→科技，并补「药」字正则）；⑦ `migrate()` 追加 `2.1.0` 步骤（SCHEMA_VERSION 升 2.1.0），按代码/名称把旧 `cat:'QDII'` 静默重分类为行业+tags，用户真实 localStorage 旧数据会自动升级。**验证**：语法校验通过；`migrate` 重分类 6 用例 + `guessCat` 6 用例全 PASS。

> ✅ **已实施**：第 9 项 眼睛图标绑定总资产（2026-08-05）。顶部导航栏的 👁 按钮已移除，**改绑到总览卡「总市值」固定 label 之后**（`<button class="eye-inline" onclick="toggleHidden()">`），而非动态金额数字——切换 `****` 时图标位置固定、不再水平抖动（避免 Layout Shift）。`toggleHidden()` 与 `init()` 由 `getElementById('eyeBtn')` 改为 `document.querySelectorAll('.eye-inline')` 批量同步所有图标（🙈/👁），删除失效的 `#eyeBtn` CSS 规则。掩码仍由 `body.masked` 全局 CSS 控制，整页金额统一隐藏。

> ✅ **已实施**：第 13 项 卖出成本 FIFO 重算（2026-08-05）。`buildFundsView` / `buildLiquidatedView` / `buildStocksView` 三处卖出逻辑由「移动加权成本」升级为**严格先进先出批次核算**：买入时按 `(金额+手续费)/份额` push `{sh,cp}` 批次，卖出时从最早批次逐个消耗、逐批计 `(卖出净值-cp)*消耗份额`，整笔手续费在末笔扣减；现金分红按 `fac=(cost-amt)/cost` 同比例下调各批次 cp；红利再投/拆分管线同步适配。已实现盈亏数字因此改变（对多次买卖、成本波动大的基金更贴近真实）。**验证**：Node 语法校验通过；FIFO 三用例（多批次/含费/分红后卖）数值全部符合手算预期。

> ✅ **已实施**：第 5 项 每日快照 daily_snapshots（2026-08-05）。数据根新增 `daily_snapshots:[]`，SCHEMA_VERSION 升 `2.2.0` 并追加 `2.2.0` MIGRATIONS 步骤（旧数据静默补空数组）；`freshData`/`silentSave`/`saveLocal`/`exportJSON`/`importJSON`/`init` 全链路接入 `dailySnapshots`。新增 `recordSnapshot()`：刷新(init/refresh)时按 `日期` 去重落一条结存 `{date,totalAssets,totalProfit,dayProfit}`（同日只更新，dayProfit=较上一条变动），上限 400 条防无限增长，立即 `silentSave` 回写。总览新增「资产走势」mcard：内联 SVG sparkline 展示 `totalAssets` 走势（涨绿跌红），无 ≥2 条记录时显 `—`。**验证**：Node 语法校验通过；recordSnapshot 去重+dayProfit 四用例（首条 null / 同日更新 / +100 / −50）全部符合预期。

> ✅ **已实施**：第 10 项 大盘看板精简（2026-08-05）。`INDEX_LIST` 由 12 只精简为 **7 只核心指数**（A股区：上证/创业板/科创50；港股区：恒生；美股区：道琼斯/纳斯达克/标普500），移除无免费实时源、常显 `— 无分时` 的破损全球外盘组（日经/韩国/富时/印度/越南）。保留 A股/港股/美股 三分组紧凑呈现；大盘看板本就只在首页/详情渲染（交易/债基 Tab 无索引元素，`renderIdxInto` 早返回），无额外改动。

> ℹ️ **评估结论（P2 第 2、3、4 项）**：
> - 第 4 项（手续费+分红）：**已满足**——记账表单已有 `费用(元)` 输入框并写入 `t.fee`（FIFO 已用），`TRADE_TYPES` 已含 `现金分红/红利再投/拆分/转换转入/转换转出`，无需再改。
> - 第 3 项（单一事实源）：**设计层面已满足**——`buildFundsView` 在有交易流水的基金上本就以 `trades` 推算份额/成本/已实现盈亏，`snaps` 仅作盘中净值缓存；若彻底移除 `snaps` 持仓角色，会让大量"纯快照录入、无交易流水"的基金丢失市值，得不偿失，故不做破坏性重写。
> - 第 2 项（债券动态化）：**保持现状**——债基走独立的 `bondState`（金额/买入日/锁定期）体系，并不在 `snaps`/`_allHoldings` 中；若按清单原案 `getBondFunds()= _allHoldings.filter(cat==='混合/债')` 直接过滤，会因债基不在主持仓里而**清空整个债券专区**。债券模块的 y1/成立以来/锁定期/同公司互转等元数据无法从持仓推导，强行动态化会退化功能。如需"持仓联动"，更合理做法是单开一个 `bondHoldings` 录入入口，属新增功能而非本次重构，故本次不动。

> ✅ **已实施**：第 7 项 多币种轻量预留（2026-08-05）。`buildFundsView` 返回对象新增 `f.currency = (snap&&snap.currency)||'CNY'`，详情页头部加 `¥ CNY` 标签（`.tag-cur` 紫色样式）；底层结构已预留跨境币种字段，后续接汇率换算无需再动数据模型。QDII 仍以人民币计价，仅作扩展接口。

> ✅ **已实施**：第 18 项 持仓列表动态搜索（2026-08-05）。顶部新增搜索框 `<input id="holdSearchInput" oninput="setHoldSearch">`（`holdSearch` 状态变量），`renderHomeList` 按**基金名称 + 代码**(`syncCode`) 模糊过滤（不区分大小写）。`setHoldSearch` 仅当停留在「持仓」Tab 时即时重渲染列表，不打断分析/债券/个股视图。CSS 新增 `.hold-search` 紧凑样式。

> ✅ **已实施**：第 17 项 饼图紧凑化 + 反向下钻（2026-08-05）。`renderPie` 环形图 `onClick` 捕获点击扇区 → `drillToCat(labels[i])`（即 `filterCat(c)+switchTab(0)`），下方图例改为可点击项 `onclick="drillToCat('...')"`，并高亮当前筛选分类（`.pie-leg.on`）。点击某板块即下钻筛选对应持仓，再点「全部」复位。CSS 新增 `.pie-leg`/`.pie-leg.on` 指针与高亮态。

> ✅ **已实施**：第 16 项 上下文快捷记账抽屉（2026-08-05）。右下角常驻 FAB「＋」→ 右侧滑入抽屉（`openDrawer/closeDrawer/drawerSave`）：基金名称（datalist 自动带出已有基金）+ 账户 + 类型（TRADE_TYPES 全类型）+ 日期（默认今天）+ 时间 + 金额 + 净值 + 费用 + 份额。保存即 `trades.push`（与详情页表单同模型、locked:true）→ `silentSave → compute → recordSnapshot`，若在持仓 Tab 则即时刷新列表与总览，无需跳详情；连续记账自动清空金额字段。打印态隐藏 `.fab/.drawer/.drawer-mask`。**验证**：Node 语法校验通过；抽屉 HTML/CSS/JS 三处一致，函数引用全局符号均为既有（`_allHoldings/fundsView/TRADE_TYPES/esc/toast/silentSave/compute/recordSnapshot/renderHomeList/renderOverview/typeLabel/M`）。

</details>
